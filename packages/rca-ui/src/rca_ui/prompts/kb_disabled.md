
KB MODE: kb-mcp is DISABLED for this session. Skip step 8 (KB recall
filtering); after step 7 stats, present the raw candidate list to the
user with the caveat that false alarms are not filtered. Skip step 9b/9c
(kb-mcp.remember dual-save) — save only the local report file.
