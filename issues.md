issue 1:

consume should be after http return. because it can takes so long and timeout?

issue 2:

opencode web shows it is using 
`/home/hychou/project/kb/rca-knowledge-poc`
I use the url directly from theoutput
OPENCODE_URL=http://127.0.0.1:4096/app?session=ses_21caedd3dffeZUnMzq91ayYOwu

maybe because the workspace is not a git? opencode only treat git repo as project
